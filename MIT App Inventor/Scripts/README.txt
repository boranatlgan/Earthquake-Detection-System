NOTE:

Write your API key into the script block near 'initialize global link to' block(blank string block) as seen on script1 image file. That will connect thingspeak website to your application.


Optional:
You can add a reference website in order to reach the latest news about earthquake activity. You just have to copy and paste to link into the string block near the 'Set ActivityStarter1.DataUri to' block(blank string block) as seen on script1 image file.